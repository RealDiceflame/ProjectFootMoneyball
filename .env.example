# Keep real keys in your local environment or GitHub Actions secrets, never in the website files.
ODDS_API_KEY=
OPENAI_API_KEY=
# Optional private PostgreSQL shadow storage. Never add this to docs/ or browser code.
OUTLIER_DATABASE_URL=
# Optional path to the downloaded database CA certificate for verify-full TLS.
OUTLIER_DATABASE_SSLROOTCERT=
